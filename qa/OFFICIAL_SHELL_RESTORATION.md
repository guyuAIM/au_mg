# 官网 Header / Footer 还原复核（2026-09-17）

本报告取代 NAVIGATION_MODES_REPORT.md 中旧版“数据同步、文字卡片”的视觉结论。
本轮仅修改 D:\gy\au_mg；未提交、推送或部署，未改动原工程及桌面副本。

## 追加修正：悬浮条 / 毛玻璃 / 点击菜单

- 应用户要求，渲染时排除官方底部 Enquire / Build & Price / Test Drive 悬浮条；
  官网数据快照仍保留来源记录，但初始HTML和在线同步渲染都不输出它。
- 移除mouseover/mouseout菜单控制及无JS的hover展开规则。Chrome实际将鼠标移至Vehicles，
  `:hover=true`且展开数量为0；点击后为1，再次点击后为0。
- 毛玻璃不明显的根因是官网body白底被复制到了局部包装层；仅令header包装层背景透明，
  不重定义官网nav参数，不改变footer和正文。滚动后官网/本地均为白色70%透明底、
  `saturate(1.8) blur(20px)`；截图确认透出并模糊后方图片。
- 在线实时状态live下悬浮条数量为0。新增2项回归测试，总计15项通过；15页构建验证通过。
- 新包仍需通过 `delivery/shell-packages.json` 的正文/派生CSS/sitemap逐字节检查。

## 还原方法

- 来源：MG 官网公开 `https://mgmotor.com.au/about/faqs`。
- 保留官网导航/页脚的 HTML 层级、class、主题 CSS、车型图片、logo、社交图标和版权区。
- Shadow DOM 隔离整站样式；本地脚本仅适配菜单展开、分类筛选、移动侧栏与滚动状态。
- 移除原站执行代码、事件处理器、表单和 iframe；表单按钮继续跳官网，不复制凭据或弹窗业务。
- 导航和页脚保留本项目 EV Guides & Advice 入口。移动页脚因此比官网多一行（32px）。
- 官方字体以 MgOfficialFavorit 别名注册，包含官网 350 字重；不修改正文 Favorit 字体。
- 修复 body 规则嵌入后裁切菜单、100vw 背景横向溢出、移动侧栏未锁滚动导致15px错位。
- 初始快照具备本地图片和样式；在线读取最新官网结构及样式，失败保留快照。不能保证未来任意官网重构无需维护。

## 本轮实测

- 13 项自动测试通过；原始数据和 CSS 哈希校验通过；15 页完整静态正文、表格、来源、canonical 与 sitemap 构建校验通过。
- 本地真实 Nginx 188 项检查通过，新增官网资源逐文件验证200；原有301、404与官网路径不接管规则通过。
- Chrome 在线 header/footer 均为 live，未记录 error/warn。
- 桌面1280×900：header高110px，logo位置(16,30)、尺寸50×50；车型菜单上沿70px、高685px。
  首张车型卡片位置(70,171.1953125)、尺寸255×173.5，与官网一致；图片和模糊背景已视觉核对。
- 桌面footer：官网/本地均高695.953125px，版权区高112.5px；首列标题相对位置
  (77.5,126.953125)、首个链接(77.5,171.953125)，链接宽35.9453125、高21.5；
  TikTok图标相对位置(1137.5,16)、尺寸30×30，均一致。
- 移动390×844：展开侧栏宽390px、左边0；首个菜单链接(16,116)、尺寸343×51，与官网一致。
  展开14个车型、ELECTRIC筛选显示7个车型、关闭恢复页面滚动均实测。
- 移动footer共同元素首链接相对Y=208.453125，与官网一致；整体增加32px仅为指南入口一行。
  桌面与移动均无横向溢出。页脚对照截图见 `delivery/restoration/`。
- 禁JS的本地HTTP页仍存在正文、图片和静态外壳；不声明所有导航交互在禁JS时等价。
- 两种最终ZIP的15页main HTML、正文派生CSS和sitemap与此前验收基准逐字节对比，结果以
  `delivery/shell-packages.json` 为准。当前导航外壳不使用旧版全页截图作为验收证据。

## 验证边界

截图工具会缩放并输出有损图像；本轮采用视觉核对与关键元素几何/字体测量，不声称全页逐像素零差异。
file://自动化受工具限制，未绕过；本地资源及路径静态检查通过，双击完整交互仍需人工复核。
未在客户正式域名部署，CSP、跨域许可和官网入口需客户接入时确认。
