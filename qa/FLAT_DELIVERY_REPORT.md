# 固定目录、完整根入口交付验收（2026-09-17）

## 当前产物与边界

`dist/index.html` 直接包含完整指南首页，不再跳转、noindex或依赖JS加载正文。
14个文章slug目录、questions问答目录、assets、scripts、404.html、sitemap_evguide.xml与该入口同级。
正式域名及 `/explore/ev-guides` 路径、metadata、JSON-LD、正文内容和样式均不改变。
项目源码保留公开路由用于Astro开发，构建最后一步只重排生成文件；不修改内容金标。

Nginx首次配置 `$mg_evguide_dir` 和官网入口/子sitemap引用。后续在接入约定不变时更新固定目录。
文件夹根目录不是官网域名根目录；不覆盖MG首页、FAQ、车型、robots或主sitemap。
本轮内容合并在研发的扁平交付目录基础上完成；发布状态以当前Git提交记录为准。

## 自动验收

- 单元测试通过，包括执行真实file-navigation脚本，核验中文/空格目录下全部内容页相互链接、
  深链锚点及搜索/分类参数；这属于JS单元测试，不是file://浏览器自动化。
- 构建检查：16个正式页面，每页一个h1，metadata/JSON-LD/来源/表格/链接及金标哈希通过。
- 根入口与Astro正式指南列表产物逐字节相同，确认不是第二套人工编辑内容。
- 16页包含指南首页、Questions & Answers页及14篇文章；问答页包含16条问题并输出FAQPage结构化数据。
- sitemap恰好16个正式URL。物理根目录有XML，官网根URL有独立映射。

## Chrome实测

- `http://127.0.0.1:4330/` 原地显示完整指南首页，地址未跳转；主内容3704字符，图片正常，无横向溢出。
- 14篇文章与Questions & Answers路径均可打开；问答搜索、筛选和展开功能已验证。
- 首页EV basics筛选显示2篇；进入How do electric cars work?后，通过面包屑返回仍为2篇筛选结果。
- 禁脚本HTTP根入口和正式列表页均有完整3704字符正文与图片；没有依赖JS注入内容。
- 390×844根入口图片正常、无横向溢出；移动侧栏打开宽390px、左边0，关闭恢复滚动。
- 连续切换文章时观察到一次官网导航请求失败/超时的快照降级提示；没有本地资源加载错误。
  最终根入口header/footer均为live、正文图片正常、悬浮条数量为0。未声称完成16页全长逐像素截图比较；本轮重点验证为路由、资源、内容可达性和交互。

## 尚需确认

工具不允许file://浏览器自动化，未绕过；实际双击交互按DELIVERY.md人工清单复核。
客户正式环境的挂载目录、静态服务器映射、CSP、TLS和站长提交由官网团队首次接入时确认。
普通静态文件服务器若没有公开路径映射，根入口图片/链接可能404，应使用本工程preview或交付配置。
